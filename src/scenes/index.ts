import { BootScene } from './BootScene';
import { PreloadScene } from './PreloadScene';
import { StartScene } from './StartScene';
import { GameScene } from './GameScene';

export enum SCENES {
    BOOT = 'Boot',
    PRELOAD = 'Preload',
    START = 'Start',
    GAME = 'Game',
}

export { BootScene, PreloadScene, StartScene, GameScene };
